package lang.print.gaps.finalModuleTask;

public class NumbersPrinter {
    public void printIsPositive(int number) {
        boolean isPositive = Boolean.valueOf(number > 0);
        System.out.println(isPositive);
    }
}
